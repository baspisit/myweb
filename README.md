# PS ChemLab

Static multilingual educational and research portal built with React, Vite, TypeScript, and Tailwind CSS.

## Commands

```sh
npm install
npm run dev
npm run build
npm run test
npm run validate:content
```

Editorial content lives in `src/data`. Public downloads and media live in `public`.
