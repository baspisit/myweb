export function Tabs({ items, active, onChange }: { items: string[]; active: string; onChange: (item: string) => void }) { return <div role="tablist" className="flex gap-2">{items.map((item) => <button role="tab" aria-selected={active === item} key={item} onClick={() => onChange(item)} className="rounded-md px-3 py-2 aria-selected:bg-brand aria-selected:text-white">{item}</button>)}</div>; }
